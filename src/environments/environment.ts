export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
  multiTenant: {
    // default tenant domain used for local development when none is present
    defaultTenant: 'demo.buildaq.com'
  }
};
